-- 000013: Extend visual_exams with detailed fields for subjective refraction,
-- keratometry, IOP method, granular biomicroscopy, fundus, and ocular motility.

ALTER TABLE visual_exams
    ADD COLUMN IF NOT EXISTS subj_av_od            VARCHAR(20)   NULL,
    ADD COLUMN IF NOT EXISTS subj_av_oi            VARCHAR(20)   NULL,
    ADD COLUMN IF NOT EXISTS addition              NUMERIC(10,4) NULL,
    ADD COLUMN IF NOT EXISTS kero_k1_od            NUMERIC(10,4) NULL,
    ADD COLUMN IF NOT EXISTS kero_k2_od            NUMERIC(10,4) NULL,
    ADD COLUMN IF NOT EXISTS kero_axis_od          INTEGER       NULL,
    ADD COLUMN IF NOT EXISTS kero_k1_oi            NUMERIC(10,4) NULL,
    ADD COLUMN IF NOT EXISTS kero_k2_oi            NUMERIC(10,4) NULL,
    ADD COLUMN IF NOT EXISTS kero_axis_oi          INTEGER       NULL,
    ADD COLUMN IF NOT EXISTS iop_method            VARCHAR(30)   NULL,
    ADD COLUMN IF NOT EXISTS biomi_lids_od         TEXT          NULL,
    ADD COLUMN IF NOT EXISTS biomi_lids_oi         TEXT          NULL,
    ADD COLUMN IF NOT EXISTS biomi_conj_od         TEXT          NULL,
    ADD COLUMN IF NOT EXISTS biomi_conj_oi         TEXT          NULL,
    ADD COLUMN IF NOT EXISTS biomi_cornea_od       TEXT          NULL,
    ADD COLUMN IF NOT EXISTS biomi_cornea_oi       TEXT          NULL,
    ADD COLUMN IF NOT EXISTS biomi_ac_od           TEXT          NULL,
    ADD COLUMN IF NOT EXISTS biomi_ac_oi           TEXT          NULL,
    ADD COLUMN IF NOT EXISTS biomi_lens_od         TEXT          NULL,
    ADD COLUMN IF NOT EXISTS biomi_lens_oi         TEXT          NULL,
    ADD COLUMN IF NOT EXISTS fundus_disc_od        TEXT          NULL,
    ADD COLUMN IF NOT EXISTS fundus_disc_oi        TEXT          NULL,
    ADD COLUMN IF NOT EXISTS fundus_macula_od      TEXT          NULL,
    ADD COLUMN IF NOT EXISTS fundus_macula_oi      TEXT          NULL,
    ADD COLUMN IF NOT EXISTS fundus_vessels_od     TEXT          NULL,
    ADD COLUMN IF NOT EXISTS fundus_vessels_oi     TEXT          NULL,
    ADD COLUMN IF NOT EXISTS fundus_periph_od      TEXT          NULL,
    ADD COLUMN IF NOT EXISTS fundus_periph_oi      TEXT          NULL,
    ADD COLUMN IF NOT EXISTS motility_versions     TEXT          NULL,
    ADD COLUMN IF NOT EXISTS motility_hirschberg   TEXT          NULL,
    ADD COLUMN IF NOT EXISTS motility_cover_test   TEXT          NULL;
