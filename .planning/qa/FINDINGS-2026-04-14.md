---
status: complete
app: convision-front
api: convision-api
base_url: http://localhost:4300
started: 2026-04-14
updated: 2026-04-15
roles_tested: [admin, receptionist, specialist_ruta_con_token_admin]
---

# QA exploración — Convision (navegador MCP)

Entorno: front `http://localhost:4300` (HTTP 200), API `http://localhost:8000` (HTTP 200). Sesión previa en navegador de pruebas afectó algunos intentos de `/login` directo mientras había JWT válido.

## Resumen ejecutivo

| Métrica | Valor |
|--------|--------|
| Pantallas verificadas (carga útil) | ~12 rutas / flujos |
| Hallazgos confirmados (FAIL/GAP) | 0 (histórico: 2 resueltos) |
| Hipótesis / pendiente | 1 |
| Bloqueantes | 0 |

---

## Hallazgos (FAIL / GAP)

### QA-20260414-001 — **RESUELTO**

- **Rol:** receptionist  
- **URL:** `http://localhost:4300/receptionist/daily-report-history`  
- **Título (histórico):** Acción “Ver” solo mostraba `alert`  
- **Corrección:** Panel lateral (**Sheet**) con detalle vía `GET /api/v1/daily-activity-reports/:id`, datos normalizados; botón con `aria-label="Ver detalle del reporte"`. Archivo: `convision-front/src/pages/receptionist/DailyReportHistory.tsx`.  
- **Estado:** resuelto  

### QA-20260414-002 — **RESUELTO**

- **Rol:** receptionist (dashboard)  
- **URL:** `http://localhost:4300/receptionist/dashboard`  
- **Título (histórico):** Mensajes de depuración en consola  
- **Corrección:** Eliminados `console.log` y trazas de depuración en la carga de citas; eliminada función no usada `verifyAppointmentStatus`; se mantiene `console.error` en el `catch`. Textos UTF-8 corregidos (Catálogo, atención, formato de fecha). Archivo: `convision-front/src/pages/receptionist/ReceptionistDashboard.tsx`.  
- **Estado:** resuelto  

---

## Hipótesis / no reproducido en esta sesión

### QA-20260414-H01

- **Tema:** Clic en **Cerrar sesión** (icono en footer del layout) no redirigió a `/login` en un intento automatizado; en otro flujo, navegar a ruta protegida de otro rol sí llevó a login.  
- **Estado:** hipótesis — posible timing del MCP o foco; conviene validación manual del botón de logout.

---

## OK (sin incidencias funcionales en lo probado)

| Rol / contexto | Ruta o flujo | Notas |
|----------------|--------------|--------|
| receptionist | `/login` → `/receptionist/dashboard` | Login `receptionist@convision.com` / `password` OK |
| receptionist | `/receptionist/daily-report` | Título “Reporte Diario de Gestión”, ítem sidebar **Reporte de gestión diario** en estado activo, enlace **Registro rápido de atención** visible |
| receptionist | `/receptionist/daily-report/quick-attention` | Wizard: ítem con perfil (Preguntas → Hombre → Finalizar) incrementa contador; ítem sin perfil (Bonos entregados) salta a observación y actualiza “Bonos entregados” |
| receptionist | `/receptionist/daily-report-history` | Tabla y filtros cargan; paginación presente |
| admin | `/login` → `/admin/dashboard` | Login `admin@convision.com` OK |
| admin | `/admin/daily-reports` | Listado y filtros (fechas, asesor, jornada); ítem **Reportes Diarios** activo en sidebar |
| admin (token) | `/specialist/dashboard` | Carga “Panel del Especialista”; saludo con usuario admin — ver nota de producto |
| RBAC | recepcionista → `/admin/dashboard` | Redirección a flujo no autorizado / login (comportamiento restrictivo observado al forzar URL) |
| API | credenciales incorrectas | `POST /api/v1/auth/login` con `Accept: application/json` → 401 `{"error":"Unauthorized"}` |

**Nota de producto (no es fallo por sí solo):** un usuario **admin** puede abrir rutas bajo `/specialist/...` y ve el panel de especialista (copys “Dr. Admin”). Decidir si es deseado para soporte o si debe bloquearse por rol.

---

## Handoff al agente de corrección

- ~~**QA-20260414-001**~~ y ~~**QA-20260414-002**~~ — cerrados en código (ver sección Hallazgos).  
- Pendiente solo validación manual **QA-20260414-H01** (logout en entorno real).

**Reglas sugeridas:** si surge un hallazgo nuevo, `@convision-qa-fixer` o `@convision-qa-gap-fixer` con este archivo como fuente.
