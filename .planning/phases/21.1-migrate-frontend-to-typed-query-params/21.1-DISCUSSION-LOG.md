# Phase 21.1: Migrate Frontend to Typed Query Params - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-09
**Phase:** 21.1-migrate-frontend-to-typed-query-params
**Areas discussed:** Scope & sequencing, TypeScript filter contract, Migration strategy & rollback, Backend cleanup follow-up

---

## Scope & sequencing

| Option | Description | Selected |
|--------|-------------|----------|
| Everything s_f/s_v in one phase | All 15 services migrated within 21.1 | ✓ |
| List pages only; defer dropdowns | Migrate list pages, defer catalog/dropdown searches | |
| By domain (1-2 domains per wave) | Cut into smaller waves | |

**User's choice:** Everything s_f/s_v in one phase.

| Option | Description | Selected |
|--------|-------------|----------|
| By domain (mirror Phase 21 backend waves) | Match backend wave breakdown | |
| By page (one plan per affected page) | Smaller atomic plans | |
| Single plan for all migrations | One big plan with task-level granularity | ✓ |

**User's choice:** Single plan for all migrations.

---

## TypeScript filter contract

| Option | Description | Selected |
|--------|-------------|----------|
| One typed interface per entity (mirror Go) | Compile-time safety, exact mirror of Go domain structs | ✓ (delegated to agent) |
| Generic Record<string, string\|number\|boolean> | Loose typing, no compile-time protection | |
| Auto-generate from Go structs | Codegen step (tygo or similar) | |

**User's choice:** "lo mejor para el proyecto" — agent selected: One typed interface per entity (mirror Go). Rationale: matches Phase 21 backend precedent, minimal new toolchain, builds on existing co-located service params pattern.

| Option | Description | Selected |
|--------|-------------|----------|
| Co-located with each service file | Inline or sibling `.types.ts` | ✓ (delegated to agent) |
| Centralized in src/services/filters/ | New folder grouping all filter interfaces | |
| In a shared @/types/api directory | Treat filters as part of API types | |

**User's choice:** "lo mejor para el proyecto" — agent selected: Co-located with each service file. Rationale: matches existing service convention (`SupplierListParams` etc. are already inline), zero new structure, lowest change footprint.

| Option | Description | Selected |
|--------|-------------|----------|
| Single `search` field (Go decides columns) | FE sends `?search=foo`; backend OR-ILIKE fans out | ✓ |
| Keep per-field filters as fallback | FE can send `?search=foo` OR `?name=foo&email=bar` | |

**User's choice:** Single `search` field.

---

## Migration strategy & rollback

| Option | Description | Selected |
|--------|-------------|----------|
| Direct cut-over (replace s_f/s_v in one commit) | Atomic per-service replacement | ✓ |
| Dual-emit transition helper | Send both shapes during transition | |

**User's choice:** Direct cut-over.

| Option | Description | Selected |
|--------|-------------|----------|
| Manual QA per page | Fast, no regression net | |
| Network-diff snapshot before/after | Capture URL+response, compare after | ✓ |
| Add Playwright/RTL tests for affected pages | Lasting regression coverage | |
| Manual QA + smoke playwright on top 3 pages | Pragmatic balance | |

**User's choice:** Network-diff snapshot before/after.

| Option | Description | Selected |
|--------|-------------|----------|
| Atomic per-service commits, revert one service | Isolate rollback per service | |
| Phase-level revert (single big commit) | Treat phase as one atomic change | ✓ |

**User's choice:** Phase-level revert.

---

## Backend cleanup follow-up

| Option | Description | Selected |
|--------|-------------|----------|
| Add backend cleanup task to 21.1 | Verification grep + assertion locks retirement | ✓ |
| Leave silent tolerance alone | Skip verification | |
| Add a follow-up phase 21.2 for backend hardening | Defer assertions to a separate phase | |

**User's choice:** Add backend cleanup task to 21.1.

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — audit all list calls | Reconcile every list call against Go domain Filter structs | ✓ |
| No — only s_f/s_v in scope | Limit phase strictly to JSON-array retirement | |

**User's choice:** Yes — audit all list calls.

---

## the agent's Discretion

- Naming convention for filter interfaces (default to Go struct name, e.g. `PatientFilter`)
- Inline vs sibling `.types.ts` per service
- Order of migration within the single plan
- Optional shared `buildQueryString<T>` helper
- Snapshot tooling for network-diff verification

## Deferred Ideas

- Backend middleware that 400s on legacy params (future hardening phase)
- TS codegen toolchain from Go domain structs (revisit if drift recurs)
- Playwright/RTL regression coverage for filter UI
- Response-shape refactors and generic list hooks
