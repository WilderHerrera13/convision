---
phase: 21.1-migrate-frontend-to-typed-query-params
plan: "02"
subsystem: frontend
tags: [react, typescript, api, filter-migration, catalog, dropdown]

requires:
  - phase: 21.1-01
    provides: typed PatientFilter/AppointmentFilter precedent
provides:
  - Typed LensCatalogFilter, WarehouseFilter, InventoryItemFilter interfaces
  - Zero s_f/s_v/s_o in all catalog and dropdown services
affects: [21.1-03]

tech-stack:
  added: []
  patterns:
    - "Single `search` field replaces multi-column s_f/s_v/s_o=or — backend OR-ILIKE fans out"
    - "LensCatalogFilter per-eye prescription range pointers match domain.form tags"
    - "branch_id=0 allBranches sentinel preserved in inventoryService with comment"

key-files:
  created: []
  modified:
    - convision-front/src/services/lensService.ts
    - convision-front/src/services/catalogService.ts
    - convision-front/src/services/inventoryService.ts
    - convision-front/src/services/brandService.ts
    - convision-front/src/services/categoryService.ts
    - convision-front/src/services/supplierService.ts
    - convision-front/src/services/laboratoryService.ts
    - convision-front/src/services/userService.ts

key-decisions:
  - "Dropped lensAttributes.material_id/lens_class_id/treatment_id from searchLenses — no typed equivalent in LensCatalogFilter; documented for 21.1-03 audit"
  - "Dropped category.slug s_f filter — backend /products endpoint filters by product_type via LensCatalogFilter"
  - "Preserved userService branch_id alias (Phase 21-10 exception to D-13)"
  - "Preserved inventoryService branch_id=0 allBranches sentinel for super-admin views"

requirements-completed: []

duration: ~10min
completed: 2026-05-09
---

# Phase 21.1 Plan 02: Migrate catalog and dropdown services Summary

**Cut over the lens/catalog/inventory/brand/category/supplier/laboratory/user services from legacy s_f/s_v JSON-array contract to flat ?key=value params matching Go domain Filter struct form tags.**

## Performance

- **Duration:** ~10min
- **Started:** 2026-05-09
- **Completed:** 2026-05-09
- **Tasks:** 5
- **Files modified:** 8

## Accomplishments

- **lensService:** `LensCatalogFilter` interface added (mirroring domain.LensCatalogFilter: brand_id, supplier_id, search + per-eye prescription range pointers). `searchLenses` now emits flat `brand_id` and `search` — dropped `lensAttributes.material_id`/`lens_class_id`/`treatment_id` (no typed backend equivalent). Four dropdown searches (brands/materials/lensClasses/treatments) now emit flat `search` instead of s_f/s_v.
- **catalogService:** `getLenses` uses flat `search` instead of s_f/s_v arrays. Dead searchFields/searchValues code removed.
- **inventoryService:** `getWarehouses` status filter uses flat `status`. `getInventoryItems` uses flat `warehouse_id`, `warehouse_location_id`, `status`. `branch_id=0` allBranches sentinel preserved.
- **5 remaining services:** brandService, categoryService, supplierService, laboratoryService, userService — all use flat `search` key. s_f/s_v interface fields removed from brand/category/supplier services. userService branch_id alias preserved (Phase 21-10 exception to D-13).

## Task Commits

1. **T1:** `cb2cc33` — refactor(21.1-02): migrate lensService to typed LensCatalogFilter
2. **T2:** `dc7f8a9` — refactor(21.1-02): migrate catalogService to flat search key
3. **T3:** `3a4d577` — refactor(21.1-02): migrate inventoryService to flat typed keys
4. **T4:** `5c13b68` — refactor(21.1-02): migrate brand/category/supplier/lab/user services to flat search
5. **T5:** (pending commit)

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None. Build exits 0, tsc passes clean. Lint has pre-existing errors in untouched files only.

## Next Phase Readiness

Ready for Plan 21.1-03 (audit alignment + lock retirement).
