# Plan 10-04: Admin entity lists batch 3 — LaboratoryOrders, DailyReports, Expenses, Payrolls, ServiceOrders, DiscountRequests, CashCloses, CashClosesByAdvisor

**Phase:** 10 — Unified Table Design — Entity Tables
**Plan:** 04 of 05
**Wave:** 2
**Depends on:** Plan 10-01 (EntityTable defaults must be updated first)
**Estimated scope:** 8 pages to migrate

---

## Goal

Migrate eight remaining admin entity list pages to use the centralized `EntityTable` component. These cover operational and financial modules: laboratory orders, daily reports, expenses, payroll, service orders, discount requests, cash closes, and advisor cash closes summaries.

## Success Criteria

- [ ] `LaboratoryOrders.tsx` uses `<EntityTable>`
- [ ] `DailyReports.tsx` uses `<EntityTable>`
- [ ] `Expenses.tsx` uses `<EntityTable>`
- [ ] `Payrolls.tsx` uses `<EntityTable>`
- [ ] `ServiceOrders.tsx` uses `<EntityTable>`
- [ ] `DiscountRequests.tsx` (admin) uses `<EntityTable>`
- [ ] `CashCloses.tsx` uses `<EntityTable>`
- [ ] `CashClosesByAdvisor.tsx` uses `<EntityTable>`
- [ ] Zero new TypeScript errors introduced in any of the eight files
- [ ] Pages using EntityTable already (LaboratoriesListPage, SuppliersListPage, UsersListPage) compile cleanly with the new EntityTable defaults (no prop conflicts)

---

## Tasks

<task type="sequential">
  <name>Verify existing EntityTable pages compile with new defaults</name>
  <read_first>
    - convision-front/src/pages/admin/laboratories/LaboratoriesListPage.tsx
    - convision-front/src/pages/admin/suppliers/SuppliersListPage.tsx
    - convision-front/src/pages/admin/users/UsersListPage.tsx
  </read_first>
  <action>
    Read each of these three files. If any of them explicitly passes `tableLayout='default'`, `ledgerBorderMode='grid'`, or `paginationVariant='default'` to EntityTable, remove that explicit prop (the default is now the correct value). If they pass `tableLayout='ledger'`, `ledgerBorderMode='figma'`, or `paginationVariant='figma'` explicitly, also remove those (redundant now that defaults match).

    Run:
    ```bash
    cd convision-front && npx tsc --noEmit 2>&1 | grep -E "Laboratories|Suppliers|Users"
    ```

    No errors expected. If any, fix the prop types.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit 2>&1 | grep -E "LaboratoriesListPage|SuppliersListPage|UsersListPage"` returns no errors
  </acceptance_criteria>
</task>

<task type="parallel">
  <name>Migrate LaboratoryOrders.tsx (admin) to EntityTable</name>
  <read_first>
    - convision-front/src/pages/admin/LaboratoryOrders.tsx (full file)
    - convision-front/src/services/laboratoryOrderService.ts (or equivalent)
    - convision-front/src/pages/admin/Patients.tsx (canonical migration pattern)
  </read_first>
  <action>
    Apply the standard migration pattern:
    1. Read the file — identify columns, service call, filter state (status, date range, lab), dialogs
    2. Remove table-management state and list useQuery
    3. Keep filter state variables at page level — pass as `extraFilters`
    4. Replace with EntityTable:

    ```tsx
    <EntityTable<LaboratoryOrder>
      columns={columns}
      fetcher={(params) => laboratoryOrderService.list({
        page: params.page,
        per_page: params.per_page,
        search: params.search,
        ...params.filters,
      })}
      queryKeyBase="laboratory-orders-admin"
      extraFilters={{
        status: filterStatus !== 'all' ? filterStatus : undefined,
        laboratory_id: filterLab || undefined,
      }}
      searchPlaceholder="Buscar orden..."
      toolbarLeading={
        <div className="flex flex-col gap-0.5">
          <span className="text-[14px] font-semibold text-[#121215]">Órdenes de Laboratorio</span>
          <span className="text-[11px] text-[#7d7d87]">Gestión de órdenes a laboratorios</span>
        </div>
      }
      toolbarTrailing={
        // CTA button or filter controls if applicable
      }
      onRowClick={(row) => navigate(`/admin/laboratory-orders/${row.id}`)}
    />
    ```

    Adjust to match actual column types, service name, filter variables, and navigation paths.
    Note: LaboratoryOrders.tsx may also use `Table` primitives for the detail view — only touch the list component.
  </action>
  <acceptance_criteria>
    - `grep -n "EntityTable" convision-front/src/pages/admin/LaboratoryOrders.tsx` returns at least 1 match
    - `grep -n "useState.*page\b" convision-front/src/pages/admin/LaboratoryOrders.tsx` returns 0 table-state matches
    - `npx tsc --noEmit 2>&1 | grep "admin/LaboratoryOrders.tsx"` returns no errors
  </acceptance_criteria>
</task>

<task type="parallel">
  <name>Migrate DailyReports.tsx (admin) to EntityTable</name>
  <read_first>
    - convision-front/src/pages/admin/DailyReports.tsx (full file)
    - convision-front/src/services/dailyReportService.ts (or equivalent)
  </read_first>
  <action>
    Apply the standard migration pattern. Daily Reports may have date-range filters or user/advisor filters.
    Keep filter state at page level, pass as `extraFilters`, replace DataTable + pagination with EntityTable.

    Toolbar title: "Reportes Diarios" / subtitle: "Reportes de gestión por jornada"
  </action>
  <acceptance_criteria>
    - `grep -n "EntityTable" convision-front/src/pages/admin/DailyReports.tsx` returns at least 1 match
    - `grep -n "useState.*page\b" convision-front/src/pages/admin/DailyReports.tsx` returns 0 table-state matches
    - `npx tsc --noEmit 2>&1 | grep "DailyReports.tsx"` returns no errors
  </acceptance_criteria>
</task>

<task type="parallel">
  <name>Migrate Expenses.tsx (admin) to EntityTable</name>
  <read_first>
    - convision-front/src/pages/admin/Expenses.tsx (full file)
    - convision-front/src/services/expenseService.ts (or equivalent)
  </read_first>
  <action>
    Apply the standard migration pattern. Expenses may have category or date-range filters.
    Keep filter state at page level, pass as `extraFilters`.

    Toolbar title: "Gastos" / subtitle: "Registro de gastos operativos"
    Include CTA button if the page has a create-expense action.
  </action>
  <acceptance_criteria>
    - `grep -n "EntityTable" convision-front/src/pages/admin/Expenses.tsx` returns at least 1 match
    - `grep -n "useState.*page\b" convision-front/src/pages/admin/Expenses.tsx` returns 0 table-state matches
    - `npx tsc --noEmit 2>&1 | grep "Expenses.tsx"` returns no errors
  </acceptance_criteria>
</task>

<task type="parallel">
  <name>Migrate Payrolls.tsx to EntityTable</name>
  <read_first>
    - convision-front/src/pages/admin/Payrolls.tsx (full file)
    - convision-front/src/services/payrollService.ts (or equivalent)
  </read_first>
  <action>
    Apply the standard migration pattern.

    Toolbar title: "Nóminas" / subtitle: "Registro de pagos de nómina"
  </action>
  <acceptance_criteria>
    - `grep -n "EntityTable" convision-front/src/pages/admin/Payrolls.tsx` returns at least 1 match
    - `grep -n "useState.*page\b" convision-front/src/pages/admin/Payrolls.tsx` returns 0 table-state matches
    - `npx tsc --noEmit 2>&1 | grep "Payrolls.tsx"` returns no errors
  </acceptance_criteria>
</task>

<task type="parallel">
  <name>Migrate ServiceOrders.tsx to EntityTable</name>
  <read_first>
    - convision-front/src/pages/admin/ServiceOrders.tsx (full file)
    - convision-front/src/services/serviceOrderService.ts (or equivalent)
  </read_first>
  <action>
    Apply the standard migration pattern. Service orders may have status or type filters.

    Toolbar title: "Órdenes de Servicio" / subtitle: "Gestión de órdenes de servicio"
  </action>
  <acceptance_criteria>
    - `grep -n "EntityTable" convision-front/src/pages/admin/ServiceOrders.tsx` returns at least 1 match
    - `grep -n "useState.*page\b" convision-front/src/pages/admin/ServiceOrders.tsx` returns 0 table-state matches
    - `npx tsc --noEmit 2>&1 | grep "ServiceOrders.tsx"` returns no errors
  </acceptance_criteria>
</task>

<task type="parallel">
  <name>Migrate DiscountRequests.tsx (admin) to EntityTable</name>
  <read_first>
    - convision-front/src/pages/admin/DiscountRequests.tsx (full file)
    - convision-front/src/services/discountRequestService.ts (or equivalent)
  </read_first>
  <action>
    Apply the standard migration pattern. Discount requests may have status filters (pending/approved/rejected).

    Note: This page uses BOTH DataTable (for the list) AND possibly raw shadcn `Table` for a detail panel. Only migrate the list component; leave any inline detail tables untouched.

    Toolbar title: "Solicitudes de Descuento" / subtitle: "Solicitudes pendientes de aprobación"
  </action>
  <acceptance_criteria>
    - `grep -n "EntityTable" convision-front/src/pages/admin/DiscountRequests.tsx` returns at least 1 match
    - `grep -n "useState.*page\b" convision-front/src/pages/admin/DiscountRequests.tsx` returns 0 table-state matches
    - `npx tsc --noEmit 2>&1 | grep "admin/DiscountRequests.tsx"` returns no errors
  </acceptance_criteria>
</task>

<task type="parallel">
  <name>Migrate CashCloses.tsx to EntityTable</name>
  <read_first>
    - convision-front/src/pages/admin/CashCloses.tsx (full file)
    - convision-front/src/services/cashCloseService.ts (or equivalent)
  </read_first>
  <action>
    Apply the standard migration pattern. Cash Closes has status filters (draft/submitted/approved).

    Toolbar title: "Cierres de Caja" / subtitle: "Cierres de caja por asesor"
    Include status filter in `extraFilters` and render the filter control above or in toolbarTrailing.
  </action>
  <acceptance_criteria>
    - `grep -n "EntityTable" convision-front/src/pages/admin/CashCloses.tsx` returns at least 1 match
    - `grep -n "useState.*page\b" convision-front/src/pages/admin/CashCloses.tsx` returns 0 table-state matches
    - `npx tsc --noEmit 2>&1 | grep "CashCloses.tsx"` returns no errors
  </acceptance_criteria>
</task>

<task type="parallel">
  <name>Migrate CashClosesByAdvisor.tsx to EntityTable</name>
  <read_first>
    - convision-front/src/pages/admin/CashClosesByAdvisor.tsx (full file)
    - convision-front/src/services/cashCloseService.ts (or equivalent)
  </read_first>
  <action>
    Apply the standard migration pattern. This page shows cash closes grouped or filtered by advisor.

    Toolbar title: "Cierres por Asesor" / subtitle: "Historial de cierres de caja"
  </action>
  <acceptance_criteria>
    - `grep -n "EntityTable" convision-front/src/pages/admin/CashClosesByAdvisor.tsx` returns at least 1 match
    - `grep -n "useState.*page\b" convision-front/src/pages/admin/CashClosesByAdvisor.tsx` returns 0 table-state matches
    - `npx tsc --noEmit 2>&1 | grep "CashClosesByAdvisor.tsx"` returns no errors
  </acceptance_criteria>
</task>

---

## Verification

```bash
cd convision-front && npx tsc --noEmit 2>&1 | grep -E "LaboratoryOrders|DailyReports|Expenses|Payrolls|ServiceOrders|DiscountRequests|CashCloses"
```

Zero TypeScript errors in the eight modified files.

## Must Haves

- All eight pages render their entity list through EntityTable
- Existing filter controls remain functional and connected to EntityTable via extraFilters
- No page-level pagination state or direct list useQuery remains
- Pages already using EntityTable (Laboratories, Suppliers, Users) compile cleanly
