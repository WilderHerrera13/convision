---
plan: "08-01"
phase: 8
wave: 1
title: "Audit — Full Inventory Backend Gap Analysis vs Laravel Reference"
objective: "Produce a structured AUDIT.md gap analysis comparing every Go inventory entity against the Laravel reference, classifying each gap by severity with exact fix details."
depends_on: []
files_modified:
  - .planning/phases/08-go-inventory-backend-audit-hardening/AUDIT.md
contributes_to:
  - INV-01
  - INV-02
  - INV-03
  - INV-04
  - INV-05
autonomous: true
must_haves:
  - "AUDIT.md exists at .planning/phases/08-go-inventory-backend-audit-hardening/AUDIT.md"
  - "AUDIT.md contains sections for all 4 entities: Warehouse, WarehouseLocation, InventoryItem, InventoryTransfer"
  - "AUDIT.md contains sections for: Domain Model, Service Methods, Repository Methods, HTTP Handlers, Routes"
  - "Each gap entry has: file, function, severity (CRITICAL/HIGH/MEDIUM/LOW), expected vs actual, exact fix"
  - "Bug #1 through Bug #13 from RESEARCH.md are all present in AUDIT.md with cross-references to plan tasks"
---

<task id="08-01-T1" wave="1">
  <title>Read all Go inventory source files and the Laravel reference doc</title>
  <read_first>
    - convision-api-golang/internal/domain/inventory.go
    - convision-api-golang/internal/inventory/service.go
    - convision-api-golang/internal/platform/storage/postgres/warehouse_repository.go
    - convision-api-golang/internal/platform/storage/postgres/warehouse_location_repository.go
    - convision-api-golang/internal/platform/storage/postgres/inventory_item_repository.go
    - convision-api-golang/internal/platform/storage/postgres/inventory_transfer_repository.go
    - convision-api-golang/internal/transport/http/v1/handler_inventory.go
    - convision-api-golang/internal/transport/http/v1/routes.go
    - convision-api-golang/internal/transport/http/v1/handler.go
    - convision-api-golang/cmd/api/main.go
    - convision-api-golang/go.mod
    - docs/laravel-warehouse-inventory-module.md
    - .planning/phases/08-go-inventory-backend-audit-hardening/08-RESEARCH.md
  </read_first>
  <action>
    Read all files listed above in full. This task produces no output — it is the mandatory context-loading step before any comparison work. Confirm:
    - domain/inventory.go defines Warehouse, WarehouseLocation, InventoryItem (with ProductID), InventoryTransfer (with LensID *uint — misnamed field), and the four Repository interfaces.
    - service.go defines Service struct with no db *gorm.DB field; NewService takes 5 args (no db).
    - service.go CreateTransfer never reads or writes inventory_items rows.
    - service.go UpdateTransfer directly casts input.Status to domain type with no state machine check.
    - service.go DeleteLocation has no inventory guard (unlike DeleteWarehouse which does at L155-165).
    - service.go AdjustStock queries by product_id, picks items[0], no transaction.
    - inventory_transfer_repository.go Update writes source_location_id, destination_location_id, quantity (immutable fields).
    - inventory_transfer_repository.go filter allowlist contains "lens_id" not "product_id".
    - handler_inventory.go ListInventoryItems does NOT parse warehouse_location_id from query.
    - handler_inventory.go GetTotalStock returns two different JSON shapes (scalar vs data array).
    - handler_inventory.go AdjustInventory reads product_id not inventory_item_id.
    - routes.go has no POST /inventory-transfers/:id/complete or /cancel routes.
    - main.go calls inventorysvc.NewService without a db argument.
  </action>
  <acceptance_criteria>
    - No code is written or modified in this task.
    - Executor confirms all 13 files have been read before proceeding to T2.
  </acceptance_criteria>
</task>

<task id="08-01-T2" wave="1">
  <title>Compare Warehouse and WarehouseLocation entities against Laravel reference</title>
  <read_first>
    - docs/laravel-warehouse-inventory-module.md
    - convision-api-golang/internal/domain/inventory.go
    - convision-api-golang/internal/inventory/service.go (Warehouse and Location sections)
    - convision-api-golang/internal/platform/storage/postgres/warehouse_repository.go
    - convision-api-golang/internal/platform/storage/postgres/warehouse_location_repository.go
    - convision-api-golang/internal/transport/http/v1/handler_inventory.go (Warehouse and Location handlers)
    - convision-api-golang/internal/transport/http/v1/routes.go (warehouse/warehouse-locations groups)
  </read_first>
  <action>
    Compare Go implementation against the Laravel reference for Warehouse and WarehouseLocation across these axes:

    WAREHOUSE:
    - Domain fields: compare Go Warehouse struct fields vs Laravel WarehouseModel fillable/casts. Note any missing fields (e.g., capacity, manager, etc.) or type mismatches.
    - Service methods: Go has ListWarehouses, GetWarehouse, CreateWarehouse, UpdateWarehouse, DeleteWarehouse (with inventory guard at L155-165), ListWarehouseLocations. Confirm which Laravel methods exist and whether all are covered.
    - Repository methods: Go WarehouseRepository interface has GetByID, Create, Update, Delete, List, ListLocations. Verify against Laravel repo pattern.
    - HTTP handlers: ListWarehouses (GET /?status=), GetWarehouse (GET /:id), CreateWarehouse (POST), UpdateWarehouse (PUT /:id), DeleteWarehouse (DELETE /:id), GetWarehouseLocations (GET /:id/locations). Compare with Laravel routes.
    - Bugs to document: UpdateWarehouse at L138-139 unconditionally sets w.Address and w.City (zero-value problem similar to Bug #8). Note severity LOW.

    WAREHOUSE LOCATION:
    - Domain fields: compare Go WarehouseLocation struct vs Laravel model.
    - Service methods: ListLocations, GetLocation, CreateLocation, UpdateLocation, DeleteLocation, validateLocationBelongsToWarehouse, ListItemsByLocation. Note: DeleteLocation (L266-271) has NO inventory guard — CRITICAL gap vs DeleteWarehouse.
    - Repository: WarehouseLocationRepository interface methods. Confirm filter allowlist covers warehouse_id, status, type.
    - HTTP handlers: ListWarehouseLocations parses warehouse_id, status from query. GetWarehouseLocation. CreateWarehouseLocation. UpdateWarehouseLocation. DeleteWarehouseLocation. ListLocationInventoryItems (GET /:id/inventory).
    - Bugs to document: DeleteLocation missing inventory guard (Bug #4, HIGH). UpdateLocation at L258 blanks Description on every update (LOW).

    For each gap found, record:
    - Entity: Warehouse or WarehouseLocation
    - File: exact file path
    - Function: exact function name
    - Severity: CRITICAL / HIGH / MEDIUM / LOW
    - Expected: what the Laravel reference specifies
    - Actual: what Go code does
    - Fix: one-sentence description of what change is needed (will be elaborated in Plan 03)
  </action>
  <acceptance_criteria>
    - No code is written or modified.
    - Executor has a populated list of gaps for both Warehouse and WarehouseLocation.
    - DeleteLocation missing inventory guard is identified as HIGH severity.
    - UpdateWarehouse/UpdateLocation zero-value fields are identified as LOW severity.
  </acceptance_criteria>
</task>

<task id="08-01-T3" wave="1">
  <title>Compare InventoryItem entity against Laravel reference</title>
  <read_first>
    - docs/laravel-warehouse-inventory-module.md
    - convision-api-golang/internal/domain/inventory.go (InventoryItem struct, InventoryItemRepository interface)
    - convision-api-golang/internal/inventory/service.go (InventoryItem section L288-494)
    - convision-api-golang/internal/platform/storage/postgres/inventory_item_repository.go
    - convision-api-golang/internal/transport/http/v1/handler_inventory.go (Inventory Items and Total Stock sections)
    - convision-api-golang/internal/transport/http/v1/routes.go (inventory-items and inventory groups)
  </read_first>
  <action>
    Compare Go InventoryItem implementation against Laravel reference across all axes:

    DOMAIN MODEL:
    - InventoryItem fields: ProductID, WarehouseID, WarehouseLocationID *uint, Quantity int, Status (InventoryItemStatus), Notes. Check against Laravel model fillable.
    - InventoryItemStatus enum: available, reserved, damaged, sold, returned, lost — confirm all values present.
    - ProductStockEntry struct: product_id, product_name, total_quantity. Check field names match Laravel JSON response.

    SERVICE METHODS:
    - ListItems, GetItem: OK.
    - CreateItem: validates location belongs to warehouse, checks uniqueness (product+location). Status enum is NOT validated against allowed values at L359 — raw string cast. Bug #7 (MEDIUM).
    - UpdateItem: Quantity field is `int` not `*int` at L305. L416 unconditionally assigns i.Quantity = input.Quantity even when client sends no quantity field. Bug #8 (MEDIUM).
    - UpdateItem: status assigned at L418 from raw string — not validated. Bug #7 applies here too.
    - DeleteItem: checks Quantity > 0 before delete. Correct.
    - TotalStock: returns scalar `{total_quantity: N}`. Bug #10 (LOW) — inconsistent with filtered shape.
    - TotalStockPerProduct: returns array, used when filters present. Inconsistent shape with TotalStock. Bug #10 (LOW).
    - ListItemsByLocation: correct.
    - GetProductInventorySummary: correct.

    REPOSITORY:
    - inventoryItemFilterAllowlist contains: product_id, warehouse_id, warehouse_location_id, status. All correct.
    - List handler (handler_inventory.go L179-201) only parses product_id, warehouse_id, status from query — NOT warehouse_location_id. Bug #11 (LOW).
    - TotalStockPerProduct joins products table using products.description as product_name — verify this matches Laravel.

    HTTP HANDLERS + ROUTES:
    - GET /inventory-items, GET /inventory-items/:id, POST /inventory-items, PUT /inventory-items/:id, DELETE /inventory-items/:id — all present.
    - GET /inventory — aliases ListInventoryItems.
    - POST /inventory/adjust — AdjustInventory handler reads product_id (Bug #5, HIGH).
    - GET /inventory/total-stock — GetTotalStock: two different shapes (Bug #10, LOW).
    - GET /products/:id/inventory-summary — GetProductInventorySummary (OK).
    - GET /warehouse-locations/:id/inventory — ListLocationInventoryItems (OK).
    - Missing: warehouse_location_id filter in ListInventoryItems query parsing (Bug #11, LOW).
  </action>
  <acceptance_criteria>
    - No code modified.
    - Executor has documented: Bug #7 (status enum, MEDIUM), Bug #8 (UpdateItem zero-value, MEDIUM), Bug #10 (TotalStock shape, LOW), Bug #11 (missing filter, LOW), Bug #5 (AdjustStock by product_id, HIGH).
  </acceptance_criteria>
</task>

<task id="08-01-T4" wave="1">
  <title>Compare InventoryTransfer entity against Laravel reference</title>
  <read_first>
    - docs/laravel-warehouse-inventory-module.md
    - convision-api-golang/internal/domain/inventory.go (InventoryTransfer struct, InventoryTransferRepository interface)
    - convision-api-golang/internal/inventory/service.go (InventoryTransfer section L496-578 + AdjustStock L580-610)
    - convision-api-golang/internal/platform/storage/postgres/inventory_transfer_repository.go
    - convision-api-golang/internal/transport/http/v1/handler_inventory.go (Inventory Transfers section L317-412)
    - convision-api-golang/internal/transport/http/v1/routes.go (inventory-transfers group L345-352)
  </read_first>
  <action>
    Compare Go InventoryTransfer implementation against Laravel reference across all axes:

    DOMAIN MODEL:
    - InventoryTransfer fields: LensID *uint gorm:"column:lens_id" (should be ProductID uint gorm:"column:product_id;not null"). Bug #9 (MEDIUM).
    - SourceLocationID, DestinationLocationID, Quantity, TransferredBy, Notes, Status, CompletedAt. Compare against Laravel model fillable.
    - InventoryTransferStatus enum: pending, completed, cancelled — all present.
    - Filter allowlist: has "lens_id" instead of "product_id". Bug #9.

    SERVICE METHODS:
    - ListTransfers, GetTransfer: OK.
    - CreateTransfer (L542-556):
      a. Does NOT check SourceLocationID != DestinationLocationID. Bug #2 (CRITICAL).
      b. Does NOT query inventory_items to verify source has sufficient stock. Bug #2 (CRITICAL).
      c. TransferCreateInput.Quantity binding:"required" accepts negative values. Bug #12 (CRITICAL, sub-item of Bug #2).
      d. Creates InventoryTransfer record without any inventory_items read or write. Bug #1 (CRITICAL) — the entire stock movement is absent.
    - UpdateTransfer (L558-571):
      a. t.Status = domain.InventoryTransferStatus(input.Status) — no state machine check. Bug #3 (CRITICAL).
      b. t.CompletedAt never set even when status changes to completed. Bug #3 (CRITICAL).
      c. Terminal states (completed, cancelled) are not protected from further mutation. Bug #3 (CRITICAL).
    - CompleteTransfer: MISSING — not implemented. Bug #1 (CRITICAL).
    - CancelTransfer: MISSING — not implemented. Bug #3 (CRITICAL).
    - DeleteTransfer: No protection against deleting a non-pending transfer. Note severity LOW.

    REPOSITORY:
    - InventoryTransferRepository.Update (L52-63) writes source_location_id, destination_location_id, quantity, lens_id — all immutable fields should not be updated. Bug #13 (MEDIUM).
    - Filter allowlist has "lens_id" instead of "product_id". Bug #9 (MEDIUM).

    HTTP HANDLERS + ROUTES:
    - GET /inventory-transfers: ListInventoryTransfers (parses status filter — OK).
    - GET /inventory-transfers/:id: GetInventoryTransfer (OK).
    - POST /inventory-transfers: CreateInventoryTransfer (does not set ProductID from input; sets TransferredBy from JWT — partially correct).
    - PUT /inventory-transfers/:id: UpdateInventoryTransfer (routes to UpdateTransfer — no state machine).
    - DELETE /inventory-transfers/:id: DeleteInventoryTransfer (OK).
    - MISSING: POST /inventory-transfers/:id/complete. Bug #1 (CRITICAL).
    - MISSING: POST /inventory-transfers/:id/cancel. Bug #3 (CRITICAL).
    - AdjustInventory handler: reads product_id not inventory_item_id. Bug #5 (HIGH).
  </action>
  <acceptance_criteria>
    - No code modified.
    - Executor has documented all critical transfer bugs: Bug #1 (stock movement absent), Bug #2 (no validation guards), Bug #3 (no state machine), Bug #9 (lens_id vs product_id), Bug #13 (repo exposes immutable fields).
    - Missing routes for /complete and /cancel are documented.
  </acceptance_criteria>
</task>

<task id="08-01-T5" wave="1">
  <title>Write AUDIT.md with structured gap analysis</title>
  <read_first>
    - .planning/phases/08-go-inventory-backend-audit-hardening/08-RESEARCH.md
  </read_first>
  <action>
    Write AUDIT.md to .planning/phases/08-go-inventory-backend-audit-hardening/AUDIT.md

    The document structure must be:

    ```
    # Phase 8 Inventory Backend Audit

    ## Summary
    | Severity | Count | Plan |
    |----------|-------|------|
    | CRITICAL | N | 08-02, 08-03 |
    | HIGH     | N | 08-02, 08-03 |
    | MEDIUM   | N | 08-03 |
    | LOW      | N | 08-03 |

    ## Warehouse
    ### Domain Model
    ### Service Methods
    ### Repository Methods
    ### HTTP Handlers & Routes

    ## WarehouseLocation
    ### Domain Model
    ### Service Methods
    ### Repository Methods
    ### HTTP Handlers & Routes

    ## InventoryItem
    ### Domain Model
    ### Service Methods
    ### Repository Methods
    ### HTTP Handlers & Routes

    ## InventoryTransfer
    ### Domain Model
    ### Service Methods
    ### Repository Methods
    ### HTTP Handlers & Routes

    ## Bug Index
    (table: Bug ID | Entity | Severity | File | Function | Plan)
    ```

    Each gap entry format within sections:
    ```
    **[BUG-N] Title** — Severity: CRITICAL/HIGH/MEDIUM/LOW
    - File: `path/to/file.go`
    - Function: `FunctionName`
    - Expected: what the behavior should be
    - Actual: what the code does today
    - Fix: which plan task addresses this (e.g., Plan 08-02-T2)
    ```

    Bug index must include all 13 bugs from RESEARCH.md plus Bug #11 (missing warehouse_location_id filter).
  </action>
  <acceptance_criteria>
    - File .planning/phases/08-go-inventory-backend-audit-hardening/AUDIT.md exists.
    - grep "CRITICAL" .planning/phases/08-go-inventory-backend-audit-hardening/AUDIT.md returns at least 5 matches (Bugs #1, #2, #3, #12 each have multiple CRITICAL entries).
    - grep "Bug #1" .planning/phases/08-go-inventory-backend-audit-hardening/AUDIT.md returns a match.
    - grep "Bug #13" .planning/phases/08-go-inventory-backend-audit-hardening/AUDIT.md returns a match.
    - grep "CompleteTransfer" .planning/phases/08-go-inventory-backend-audit-hardening/AUDIT.md returns a match.
    - grep "warehouse_location_id" .planning/phases/08-go-inventory-backend-audit-hardening/AUDIT.md returns a match.
    - grep "08-02\|08-03" .planning/phases/08-go-inventory-backend-audit-hardening/AUDIT.md returns matches (plan cross-references present).
  </acceptance_criteria>
</task>
